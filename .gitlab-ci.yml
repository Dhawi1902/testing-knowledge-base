pages:
  stage: deploy
  script:
    - pip install mkdocs-material
    - mkdocs build -d public
    - mkdocs build -f jmeter/mkdocs.yml -d public/jmeter
  artifacts:
    paths:
      - public
  only:
    - main
